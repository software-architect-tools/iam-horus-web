<!DOCTYPE hibernate-configuration PUBLIC 	"-//Hibernate/Hibernate Configuration DTD//EN"	"http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd"><!-- This hibernate configuration is used only for IMAGES_GALLERY stereotype, 	because this stereotype is implemented using hibernate native APIs. If you 	do not use IMAGE_GALLERY you do not need this file. The datasource configured 	here is the datasource in where the images will be store, and usually match 	wiht the main datasource of application defined in persistence.xml. --><hibernate-configuration>	<session-factory>		<property name="hibernate.jdbc.use_get_generated_keys">false</property>		<property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>		<property name="hibernate.connection.url">jdbc:mysql://${DATABASE_HORUS_HOST}:${DATABASE_HORUS_PORT}/${DATABASE_HORUS_NAME}</property>		<property name="hibernate.connection.username">${DATABASE_HORUS_USER}</property>		<property name="hibernate.connection.password">${DATABASE_HORUS_PASSWORD}</property>		<property name="hibernate.connection.pool_size">${JDBC_DATASOURCE_MAX_ACTIVE}</property>		<property name="hibernate.current_session_context_class">thread</property>		<property name="hibernate.show_sql">${LOG_HIBERNATE_SQL}</property>		<property name="hibernate.dialect">org.hibernate.dialect.MySQL5InnoDBDialect</property>		<!-- GalleryImage is needed only if you uses IMAGES_GALLERY/GALERIA_IMAGENES 			stereotype -->		<mapping resource="GalleryImage.hbm.xml" />	</session-factory></hibernate-configuration>